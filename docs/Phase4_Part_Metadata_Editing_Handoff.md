# Part Pilot — Existing-Part Metadata Editing Handoff

Generated by Patch 149 after automated verification and manual browser
approval.

## Source of truth

- Repository: `https://github.com/devanshtangri/Part-Pilot`
- Branch: `main`
- Checkpoint commit: `Add existing part metadata editing`
- Alembic head: `0005_packages`
- Direct handoff:
  `https://raw.githubusercontent.com/devanshtangri/Part-Pilot/main/docs/Phase4_Part_Metadata_Editing_Handoff.md`

A new chat must fetch this handoff from GitHub and inspect the current
repository before changing anything.

## Mandatory workflow

Every diagnostic, fix, or implementation must be a complete Python file.
Never require manual source editing.

Every implementation script must verify prerequisites, back up changed files,
compile Python, run `git diff --check`, build and deploy, verify Alembic, run
the complete smoke suite, verify deployed bundles/routes, roll back on failure,
and print the exact final line `Everything PASS` only after success.

Use headings:

- `Browser test — Patch N` when manual browser testing is required.
- `Fix only — Patch N` when terminal verification is sufficient.

When the user says `everything pass`, it means only that the terminal printed
`Everything PASS`; browser approval must be explicit.

Do not commit implementation until browser approval. At meaningful commits,
update `docs/Checkpoint.md`, `docs/Implementation_Roadmap.md`, and the active
handoff.

## Completed foundations

- Authentication and protected routes.
- Built-in and custom part types.
- Typed dynamic fields.
- Inventory creation and browsing.
- Manufacturer and package catalogues.
- Responsive part details.
- Quantity adjustment and recent stock history.

## Completed metadata editing

Protected API:

- `PUT /api/parts/{part_id}`

Editable:

- name and part number
- manufacturer
- package/form factor
- description and notes
- unit price and purchase link
- low-stock settings
- typed template-field values

Safeguards:

- part type fixed
- total and reserved quantity fixed
- unexpected fields rejected
- active manufacturer required when selected
- typed and required values validated
- duplicate part numbers rejected
- atomic `part.metadata_updated` audit history
- no stock movement created

Frontend:

- prefilled Edit details modal
- catalogue reuse
- typed template controls
- drawer closes while editor is open
- Cancel/close/backdrop/Escape/Save restore the drawer
- open record and Stored Parts refresh after save
- responsive layout
- insignificant decimal padding hidden in inputs without changing stored
  precision

The user manually approved the complete browser workflow.

## Git state after Patch 149

Patch 149 commits and pushes this approved batch. The tracked working tree must
be clean. An unrelated local untracked file such as
`133_inspect_stock_movement_targets.py` may remain.

## Next implementation area

Start with **Diagnostic 150** for soft deletion and restoration.

Inspect:

- `Part.is_deleted` and `Part.deleted_at`
- active list/detail filters
- stock movement and dynamic-value retention
- audit patterns
- duplicate part-number conflicts during restoration
- current details-footer actions
- confirmation-dialog styles
- deleted-record discovery and restoration entry points
- smoke-test cleanup patterns
- checkpoint and roadmap requirements

Recommended boundary:

- authenticated soft delete
- preserve part row, field values, stock movements, and audit history
- deletion audit event
- authenticated restore
- conflict-safe restoration
- restoration audit event
- focused delete confirmation and narrow restoration entry point
- no permanent deletion, quantity changes, locations, reservations, or
  projects

## First deliverable in the next chat

Create a read-only complete Python file:

`150_inspect_part_soft_delete_restore_targets.py`

It must write a report under `fixes/logs/`, make no tracked source changes, and
print `Everything PASS` only after the report is verified.
