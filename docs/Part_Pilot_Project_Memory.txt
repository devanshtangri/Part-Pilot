<!-- PARTPILOT:PROJECT_MEMORY:V633 -->

Project
- Repo https://github.com/devanshtangri/Part-Pilot.git; root /projects/Part Pilot; branch main.
- Docker compose service partpilot, host port 7890. React/Vite/TypeScript + FastAPI/SQLAlchemy/Alembic.
- Alembic head: 0015_mcp_direct_clients.

Patch workflow
- Every implementation/fix/diagnostic/docs/checkpoint/commit/push/boundary is one complete sequential Python file under fixes/. Failed attempts consume numbers; never reuse. User runs scripts; assistant never executes numbered patches.
- Success final line exactly `Everything PASS`. Validate exact repo/branch/origin/HEAD, index/source hashes, prerequisites/transforms before writes. Backups fixes/backups/; logs fixes/logs/; diagnostics docs/diagonostic_*.md.
- Browser-test source stays uncommitted until explicit approval; checkpoint/commit/push promptly after approval. Browser instructions stay outside scripts.
- After two consecutive pre-write failures/repeated anchors/source uncertainty, diagnostic-only next. Compile final downloadable patch before delivery.
- App changes: git diff --check, relevant Python compile, canonical Docker build, Alembic, copied-db complete smoke, protected APIs/SPA/runtime markers, live-data preservation/rollback.
- Exact Docker image checks require .dockerignore + Git-canonical backend/frontend modes. Rehearsals must use isolated /tmp copies and must never mutate live source/deployment.
- Keep this memory <=8000 bytes. User prefers patch-generation turns <=10 minutes.

Chat boundary
- Chat 21 `Account Security and Session Administration` completes at Patch 633. Patch 632 checkpoint commit: 88ca83cd407d63772e027cac409357f5bc192ad0.
- Chat 22 title: `Chat 22: MCP Permissions and Settings Organization`; range 634-658; first patch 634; boundary 658.
- Patch 634 should be diagnostic-only for individual-tool/per-client MCP permissions. No starting-prompt files; ready prompt only in chat after successful boundary.

Data safety
- Preserve users/sessions/inventory/Projects/Reservations/movements/audits/settings/API keys/OAuth/direct credentials/backups/restore evidence. Cleanup only exact owned IDs; never broad OAuth cleanup. SQLite integrity/FKs must pass.
- 5V Relay remains recoverable in Deleted items. ESP01 was permanently purged and Development Board Part Type deleted during approved recycle-bin testing.
- Instance-secret SHA: 19c5c9519a188a8f969bb2bd67c65da1418a73605e8747450df8ddeb44d8b47b. Never reveal/rotate credentials automatically.

Completed application
- Typed inventory/catalogues; universal search/filter/sort/pagination; quantity workflows; soft delete/restore; History; atomic Reservation/Project lifecycles.
- Responsive dark/light/system Settings; strict portable backup/restore with staging/rollback/session invalidation.
- Recycle bin: recoverable delete default, typed-confirm atomic purge, active/reserved blockers, audit retention, custom Part Type dependency protection across active + deleted inventory.
- Current-user account/security: username/display/avatar incl DB-backed normalized custom avatar, password change, safe sessions, targeted/revoke-all-other, current-session preservation, secret-free audit. Four older sessions remain Unknown; never infer/backfill.

REST API access
- Alembic 0014: scoped `pp_api_key_` one-time credentials, digest-only storage, scopes/expiry/rotate/revoke/last-used.
- 43 exact eligible route method/path scope registrations across Inventory/Catalogue/Project/Reservation/History. Browser-session fallback; Auth/Settings/Backup/Restore session-only. Invalid/revoked/expired=401; missing scope=403.
- Browser-approved API Access Settings UI: create/edit/rotate/revoke, hidden revoked records, one-time secret, scope presets, API docs actions, modal security flows. `/docs`, `/redoc`, `/openapi.json` need deliberate public-alpha access policy.

MCP/OAuth
- OAuth discovery, DCR, PKCE consent, access/refresh/revocation, protected-resource metadata, stateless `/mcp`; six read-only Part/Project/Reservation tools. Real Claude/ChatGPT read flows verified.
- Manual OAuth admin: creator ownership, public/confidential, none/secret_post/secret_basic, generated ID, one-time secret digest-only storage, status/revocation; revoked hidden from normal Settings.
- Alembic 0015: named Bearer/custom-header/trusted-network direct clients with independent identity, enable/disable, rotate/revoke and safe usage metadata. Legacy disabled row retained for audit compatibility.
- `Enable MCP server` master; `Allow direct MCP clients` gates named direct access. Instance-wide no-auth is off by default, typed-confirmed `ALLOW NO AUTH`, read-only, and lower precedence than explicit credentials/trusted networks. OAuth remains independent.
- Browser-approved MCP UI: named-client security dialogs, aligned section widths, readable single-level disabled state. Direct clients/no-auth currently OFF; MCP server/read ON; write authorization OFF.

Recovery lessons
- 628: CSS comment marker stripped by minification; use DOM/value/custom-property markers.
- 629: frozen packaged CSS hash differed from rehearsal; freeze exact script-produced bytes.
- 631: numbered preflight caught that an assistant rehearsal had accidentally applied exact intended live CSS/image. Never mutate live repo/deployment during rehearsal; isolated /tmp only.
- Canonical Docker exact-image checks also require ignored bytecode exclusion and Git-canonical file modes.

Remaining V1
1. Global individual-tool + per-client MCP permissions.
2. Broader Settings organization with restrained dividers/groups throughout relevant Settings areas.
3. Protect API docs/schema exposure before public alpha.
4. ISO currency selector using persisted app-wide currency; formatting only, no FX.
5. Server-backed Stored Parts metrics across complete active inventory: Total components=sum total_quantity; Inventory value=sum priced total_quantity*unit_price with explicit price coverage; Available, Reserved, Low stock, Out of stock, distinct Part count.
6. Preference/default restoration; multi-user Owner/Admin/Operator/Viewer roles; safeguarded MCP writes; final accessibility/security/responsive/API-MCP regression.
- Notifications & Messaging remain post-v1.
