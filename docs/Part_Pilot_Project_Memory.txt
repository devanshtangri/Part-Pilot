Part Pilot project memory
<!-- PARTPILOT:PROJECT_MEMORY:V528 -->

Project
- Repository: https://github.com/devanshtangri/Part-Pilot.git
- Root: /projects/Part Pilot; branch: main
- Compose service: partpilot; host port: 7890
- React/Vite/TypeScript frontend; FastAPI/SQLAlchemy/Alembic backend
- Alembic head: 0010_mcp_trusted_networks

Patch workflow
- Every implementation, fix, diagnostic, documentation update, checkpoint,
  commit, push and boundary is one downloadable sequential Python file.
- Never place a generated patch inside the project. Provide the download; the
  user places it under fixes/.
- Failed scripts consume numbers. Success ends with exactly `Everything PASS`.
- Validate exact repo, HEAD/origin, Git/index, source, prerequisites and all
  transforms before write.
- Backups: /projects/Part Pilot/fixes/backups/
- Logs: /projects/Part Pilot/fixes/logs/
- Diagnostic Markdown prefix: `diagonostic_`.
- Application patches build/deploy, verify Alembic, APIs/routes, copied-data
  smoke, live-data preservation and rollback.
- Browser-test source stays uncommitted until approval; checkpoint separately.
- After two consecutive pre-write failures, repeated anchor mismatches or source
  uncertainty, stop and issue a diagnostic-only patch.
- Keep this file at or below 8,000 characters.
- Never execute numbered patch scripts for the user.

Chat boundary
- Chat 19: OAuth Connector Completion and MCP Write Foundation.
- Patch range 518-547; planned boundary 547.
- No starting-prompt file. Post the ready prompt only after boundary PASS.

Inventory safety
- Preserve real users, sessions, catalogues, inventory, Projects, Reservations,
  movements, audits, settings, credentials and restore evidence.
- Six realistic Patch 401 parts remain intentionally preserved.
- Cleanup only exact manifest/allowlist IDs with before/after proof.
- Never clean OAuth rows by broad name, origin or timestamp.
- SQLite integrity and foreign-key checks must pass.

Completed application
- Authenticated inventory, typed templates, reusable catalogues, universal
  search, filtering/sorting/pagination, quantity workflows, soft delete,
  restoration, and detailed History.
- Atomic Reservation and Project reserve/release/consume/edit lifecycles.
- Responsive dark/light/system appearance and Settings for Appearance,
  Inventory, Reservations, MCP, and Data.
- Portable backup/restore with strict validation, persistent staging, rollback,
  atomic replacement and session invalidation.

Completed MCP/OAuth
- OAuth discovery, dynamic registration, PKCE consent, token rotation,
  revocation, protected-resource metadata, and stateless `/mcp`.
- Six read-only part, Project and Reservation tools.
- Global MCP enabled/read/write settings.
- Bearer, custom-header and trusted-network direct authentication.
- Encrypted keys, reveal/rotation/disable, strict proxy/client-IP resolution,
  and secret-free audits.
- Official SDK passed initialize, tools/list and search_parts through public TLS.
- OAuth page styling, duplicate-submit lock, progress, validated callback CSP,
  and error states are approved.
- Claude and ChatGPT are connected through OAuth with `mcp:read`.
- Hermes is connected through direct Bearer authentication.
- MCP/read enabled; write authorization disabled.
- Patch 527 committed and pushed the approved OAuth source.

Authoritative current state
- HEAD/origin: 017eb65d7c2fcf8622fb21d2c19cf0dd52a554ae
- Subject: Complete OAuth connector browser workflow
- Deployment image:
  sha256:a2954d8cd2f84cbd283d77d8679bc881e14f6896d0e3df737bb5e178dabbba44
- Deployment running/healthy, restart count 0.
- Alembic: 0010_mcp_trusted_networks
- Instance-secret SHA:
  19c5c9519a188a8f969bb2bd67c65da1418a73605e8747450df8ddeb44d8b47b
- Active OAuth clients must include Claude and ChatGPT with read-only scope.
- Direct Bearer is active for Hermes; never reveal/rotate/disable automatically.
- OAuth tokens may rotate; validate connected-client semantics, not stale counts.

Locked Settings/product direction
- History must display MCP, OAuth, API, HTTP/HTTPS, URL/URI, ID, IP, CIDR,
  PKCE, UI, CSV and JSON with canonical capitalization.
- Settings expands to General, Appearance, Inventory, Reservations, Account &
  Users, Integrations, Data & Maintenance, and Advanced.
- Account: username/display name/password, sessions, built-in avatars; later
  Owner/Admin/Operator/Viewer roles and multi-user administration.
- REST API keys: separate `pp_api_...` credentials, one-time display, hash-only
  storage, scopes, expiry, last use, rotation, revocation and audit.
- OAuth administration: status visibility, exact revocation, manual
  registration, generated client ID and one-time secret when required.
- Direct access: master switch; no-auth, Bearer, custom header, trusted network;
  no-auth warned/confirmed and read-only initially.
- Evolve singleton direct auth into named clients with independent credentials,
  tool policy, last use, rotation and revocation.
- MCP permissions: global category/tool ceiling plus per-client
  Inherit/Allow/Deny; Deny wins; enforce tools/list and tools/call.
- Per-client permissions precede MCP write tools.
- Expand General/Appearance/Inventory/Reservations/Data preferences.
- Each section gets Restore defaults; global restore affects preferences only.
  Credential/access reset remains separate and destructive.

Immediate order
1. Patch 528 documentation checkpoint.
2. Patch 529 History acronym normalization.
3. Exact abandoned OAuth-row diagnostic and allowlisted cleanup.
4. Connected OAuth client list/revocation.
5. Manual OAuth client registration.
6. Current-user profile/security/avatars/sessions.
7. Scoped REST API keys.
8. Direct master switch, no-auth, named direct clients.
9. Global/per-client MCP tool policy.
10. Expanded settings and restore-default controls.
11. Multi-user roles/administration.
12. Safeguarded MCP write tools.
13. Accessibility, security and public-alpha hardening.
