<!-- PARTPILOT:PROJECT_MEMORY:V790 -->

Project
- Repo: https://github.com/devanshtangri/Part-Pilot.git; root `/projects/Part Pilot`; branch `main`. Docker Compose service/container `partpilot`, usual host port 7890. React/Vite/TypeScript frontend; FastAPI/SQLAlchemy/Alembic backend; SQLite production DB `/projects/Part Pilot/data/partpilot.db` (`/data/partpilot.db` in container).
- Chat 27 public-alpha release-candidate boundary: Patch 796 over Alembic `0022_mcp_inventory_part_lifecycle`; 14 MCP tools = six reads + eight safeguarded writes. P795 is the authoritative final-gate diagnostic; P796 rehearses the five mutable-fixture adapters, reruns all 44 release smokes, preserves the approved V785 runtime and closes Chat 27. Next patch 797 in Chat 28.

Patch workflow
- Every implementation/fix/diagnostic/docs/checkpoint/commit/push/boundary/handoff is one sequential Python file under `fixes/`; filename starts directly with the patch number. Failed executed scripts consume numbers. Successful final line exactly `Everything PASS`.
- User normally runs numbered patches. Assistant may execute one only when explicitly asked to run that exact patch; then run the exact command once and return raw terminal output only, with no diagnosis/retry/extra commands.
- Before writes validate repo/root, branch, verified origin identity, committed HEAD, exact index/source bytes, prerequisites and transforms. Backups `fixes/backups/`; logs `fixes/logs/`; diagnostics `docs/diagonostic_*.md`. Avoid brittle whitespace anchors.
- Browser-test app source remains uncommitted/unpushed until explicit approval. Browser instructions stay outside patch scripts. After approval create a separate checkpoint/commit/push patch promptly.
- App changes require `git diff --check`, relevant compile/build, canonical Docker build, Alembic validation, copied-production/relevant smokes, protected APIs, SPA/runtime markers and real-data preservation. Rehearsals stay isolated from live source/deployment.
- Stage only an exact allowlist; verify staged diff/check; push `main`, fetch and verify local HEAD == `origin/main`. After two consecutive pre-write failures/repeated anchor mismatch/uncertain pending source, diagnostic-only next.
- Origin spelling must match the verified configured remote; Patch 767 proved equivalent HTTPS-vs-SSH spelling can cause a false preflight failure. Repo memory target <=20,000 characters. Newest handoff + local repo state are authoritative for volatile hashes/runtime/pending state.
- Patch delivery order: heading; file/path; SHA-256; scope/source-build-browser-commit status; exact run command; expected `Everything PASS`. For AI/MCP browser tests provide exact copy-paste prompts.

Chat boundary
- Chat 27 authoritative boundary recovery is Patch 796 after the planned Patch 793 boundary failed and narrow diagnostic/recovery continued through P795/P796.
- Chat 28: `Public Alpha Release and Post-v1 Planning`, patch range 797-821, planned boundary 821.

Data safety
- Preserve real inventory, users, sessions, Projects, Reservations, movements, audits, settings, API keys, OAuth/direct-client credentials, backups and restore evidence. Test only unique/manifest-owned fixtures; never broad-delete.
- Live DB/settings/OAuth/client/tool-permission changes from user activity are legitimate mutable state. Do not pin old DB hashes or permission booleans and never reveal/rotate credentials automatically.
- Existing MCP browser fixture: part ID 16, `MCP-P756-CREATE-TEST`, name `Patch 760 MCP Metadata Test`, Resistor, physical 12, reserved 0, three typed fields. It is legitimate live evidence; do not normalize/delete automatically.

Product/UI rules
- Flat dense enterprise UI; subtle borders, restrained radii, readable spacing and responsive behavior; no gradients/glow/glass/decorative imagery. Prefer themed controls. Disabled = not-allowed; wait/busy only during actual loading/saving.
- Preserve search/filter/sort/pagination/stale-request guards/selection/history/edit/delete/restore behavior through layout changes; explicitly test intermediate/mobile widths.
- Low-risk reversible Preferences may autosave with rollback/stale-request protection. Credentials/password/delete/purge/revoke/rotate/backup-restore/lifecycle transitions require explicit actions/confirmation.
- Currency is formatting/display only and timezone is passive timestamp display only; neither rewrites historical prices/currency snapshots/stored timestamps.

Core application
- Typed inventory/catalogues; universal search/filter/sort/pagination; stock movements; recycle-bin soft delete/restore plus separate confirmed purge; system History; atomic Projects/Reservations.
- Auth/profile/sessions; permanent first-init Primary Owner; Administrator/Operator/Viewer ceilings; responsive Users & Roles; unauthorized Settings workspaces hidden; REST API keys; backups/restores; responsive dark/light/system Settings.
- Authenticated SSE + cross-tab relay drives targeted live refetch across Inventory/History, Projects/Reservations, Dashboard, Settings/account/backups and API/MCP integrations while preserving local drafts/search/filter/page/selection.
- Dashboard focuses universal search, whole-inventory metrics, activity and Quick actions; the redundant Stock alerts launcher/dialog is removed. Routine Refresh buttons remain removed while error Retry remains.
- Projects/Reservations use aligned horizontal register scrolling at intermediate widths so Updated timestamps remain reachable; <= mobile breakpoint retains cards. History Part-related records can open the exact Inventory Part drawer through `/inventory?part=<id>`.
- Settings uses a shared grouped information architecture across Account, Users, Preferences, API, MCP and Data. Built-in avatars are fixed square choices. Sequential number tags are removed; 16 restrained semantic SVG badges provide landmarks. Generated API/MCP/OAuth values use in-field Copy controls.

MCP/OAuth/direct access
- OAuth discovery/DCR/PKCE/consent/token/refresh/revocation and stateless `/mcp`; real Claude/ChatGPT flows verified. Scope challenges reflect enabled categories; existing tokens never silently gain `mcp:write`.
- Named direct clients support Bearer/custom-header/trusted-network auth. No-auth is off by default, typed-confirmed, read-only and lower precedence than explicit credentials/trusted networks.
- Global individual-tool permissions + per-client deny ceilings + server/read/write category gates apply at discovery and call time. Permission booleans remain mutable configuration.
- Safeguarded writes reuse canonical services and require server/write/global/client/scope ceilings, active Operator-or-higher authority, preview, short-lived confirmation, idempotency/completed replay and state-drift rejection. No-auth stays read-only.
- Writes: `reserve_project`, `consume_reservation`, `cancel_reservation`, `adjust_part_quantity`, `create_part`, `update_part_metadata`, `soft_delete_part`, `restore_part`. Permanent purge/hard delete/recycle-bin emptying is deliberately absent from MCP.
- `update_part_metadata` is complete explicit metadata replacement and cannot change stock. `soft_delete_part`/`restore_part` reuse recycle-bin services, preserve quantities/reservations/fields/movements/history and create no stock movement solely for lifecycle change. History shows connected MCP client identity while retaining backing user accountability.

Release evidence / remaining Chat 27
- Primary Owner/User management is browser-approved and checkpointed. Patch 777 passed 44 current release smoke invocations on fresh copied-production DBs plus canonical build, restore, OpenAPI, SPA and MCP regression; restore-commit concern was harness env drift and passes with `PARTPILOT_RESTORE_SUPERVISOR_CONTRACT=compose-restart-v1`.
- Real Claude OAuth verification exposed six reads + eight safeguarded writes, no hard-delete tool, and successfully exercised guarded metadata preview/confirmation. Hermes `MCP event loop is not running` is client/runtime-wrapper-side: the same public Part Pilot endpoint initialized and executed `search_parts` from inside the Hermes container, so Part Pilot should not add a workaround.
- Patch 779-785 browser polish is approved: Dashboard cleanup, Projects/Reservations responsive registers, History Part deep-link, in-field credential Copy, MCP and all-Settings hierarchy, avatar tile sizing, semantic icons and Connections optical correction.
- Remaining work: final public-alpha release-candidate durable handoff/documentation and only genuinely new release blockers before the planned Patch 793 boundary. Permanent purge stays separate from MCP; Notifications & Messaging remain post-v1.

<!-- PARTPILOT:PUBLIC_ALPHA_RELEASE_POLISH_MEMORY:V786 -->

<!-- PARTPILOT:PUBLIC_ALPHA_RELEASE_HYGIENE_MEMORY:V790 -->
- Public-alpha release hygiene now documents HTTPS/proxy/no-auth/data/reset/backup safety and removes stale pre-MCP wording without changing live configuration. Licensing remains an explicit repository-owner decision before the final Chat 27 boundary.

<!-- PARTPILOT:CHAT27_PUBLIC_ALPHA_BOUNDARY_MEMORY:V796 -->
- Patch 796 closes Chat 27 after targeted rehearsal of all five diagnosed mutable MCP fixtures and all 44 release smokes pass. Fixture normalization is copy-only: four historical singleton smokes retain only legacy row 1; the named-direct smoke gets copied disabled defaults. Live direct clients/settings/permissions remain mutable and untouched. Approved image `sha256:fa265f69c32b784172f8fc46819ea484c0170dbd4df13b3c8fbd6bcd86711f37`, Alembic `0022_mcp_inventory_part_lifecycle`, browser-polished Settings/User UI and Claude external MCP evidence remain authoritative. Next patch 797. License decision remains explicit.
