<!-- PARTPILOT:PROJECT_MEMORY:V680 -->

Project
- Repo https://github.com/devanshtangri/Part-Pilot.git; root /projects/Part Pilot; branch main.
- Docker compose service partpilot, host port 7890. React/Vite/TypeScript + FastAPI/SQLAlchemy/Alembic.
- Alembic head: 0020_mcp_inventory_part_create.

Patch workflow
- Every implementation/fix/diagnostic/docs/checkpoint/commit/push/boundary is one sequential Python file under fixes/. Failed attempts consume numbers. Assistant runs a numbered patch only when the user explicitly asks to run that specific patch; otherwise the user runs it. On an explicit run request, execute exactly that requested run command once and return the full raw terminal output only; no conclusions, diagnosis, fixes, retries, inspections or extra commands unless separately asked.
- Success final line exactly `Everything PASS`. Validate exact repo/branch/origin/HEAD, index/source hashes, prerequisites/transforms before writes. Backups fixes/backups/; logs fixes/logs/; diagnostics docs/diagonostic_*.md.
- Browser-test source stays uncommitted until explicit approval; checkpoint/commit/push promptly after approval. Browser instructions stay outside scripts.
- After two consecutive pre-write failures/repeated anchors/source uncertainty, diagnostic-only next. Compile final downloadable patch before delivery.
- App changes: git diff --check, relevant Python compile, canonical Docker build, Alembic, copied-db complete smoke, protected APIs/SPA/runtime markers, live-data preservation/rollback.
- Exact Docker image checks require .dockerignore + Git-canonical backend/frontend modes. Rehearsals must use isolated /tmp copies and must never mutate live source/deployment.
- Repo durable memory is separate from ChatGPT Project Memory. ChatGPT Project Memory holds stable global rules and stays within its separate 8K product limit; this file may hold richer durable state. Keep this repo file <=20000 characters to control startup context. User prefers patch-generation turns <=10 minutes.

Chat boundary
- Chat 25 `Integration Live Sync Recovery and Public Alpha Hardening` began at 711 with planned boundary 735; a narrow MCP-write recovery sequence overran the boundary. Patch 742 is the first safe approved checkpoint after Patch 741 boundary-validation recovery and closes Chat 25.
- Chat 26 `MCP Inventory Writes and Public Alpha Finalization`: range 743-767, first 743, boundary 767.
Data safety
- Preserve users/sessions/inventory/Projects/Reservations/movements/audits/settings/API keys/OAuth/direct credentials/backups/restore evidence. Cleanup only exact owned IDs; never broad OAuth cleanup. SQLite integrity/FKs must pass.
- 5V Relay remains recoverable in Deleted items. ESP01 was permanently purged and Development Board Part Type deleted during approved recycle-bin testing.
- Instance-secret SHA: 19c5c9519a188a8f969bb2bd67c65da1418a73605e8747450df8ddeb44d8b47b. Never reveal/rotate credentials automatically.

Completed application
- Typed inventory/catalogues; universal search/filter/sort/pagination; quantity workflows; soft delete/restore; History; atomic Reservation/Project lifecycles.
- Responsive dark/light/system Settings; current reversible defaults are consolidated under Preferences: Theme, Inventory display and Reservation defaults. Each autosaves independently and has its own targeted Reset-to-default action; reset preserves unrelated preferences/security/business data. Strict portable backup/restore retains staging/rollback/session invalidation.
- Responsive regression: Projects lifecycle headers stay readable; navigation drawer covers 821-1080px while preserving <=820px compact behavior.
- Recycle bin: recoverable delete default, typed-confirm atomic purge, active/reserved blockers, audit retention, custom Part Type dependency protection across active + deleted inventory.
- Account/security: username/display/avatar incl custom DB image, password change, safe session revoke flows, current-session preservation, secret-free audit. Four older sessions remain Unknown; never infer/backfill.

REST API access
- Alembic 0014: scoped `pp_api_key_` one-time credentials, digest-only storage, scopes/expiry/rotate/revoke/last-used.
- 43 exact eligible route method/path scope registrations across Inventory/Catalogue/Project/Reservation/History. Browser-session fallback; Auth/Settings/Backup/Restore session-only. Invalid/revoked/expired=401; missing scope=403.
- Browser-approved API Access Settings UI: create/edit/rotate/revoke, hidden revoked records, one-time secret, scope presets, API docs actions, modal security flows. `/docs`, `/redoc`, `/openapi.json` need deliberate public-alpha access policy.

MCP/OAuth
- OAuth discovery, DCR, PKCE consent, access/refresh/revocation, protected-resource metadata, stateless `/mcp`; six read Part/Project/Reservation tools plus five safeguarded writes. Real Claude/ChatGPT flows verified.
- Manual OAuth admin: ownership, public/confidential auth, generated IDs, digest-only one-time secrets, status/revocation; revoked hidden from normal Settings.
- Alembic 0015: named Bearer/custom-header/trusted-network direct clients with independent identity, enable/disable, rotate/revoke and safe usage metadata. Legacy disabled row retained for audit compatibility.
- `Enable MCP server` master; `Allow direct MCP clients` gates named direct access. Instance-wide no-auth is off by default, typed-confirmed `ALLOW NO AUTH`, read-only, and lower precedence than explicit credentials/trusted networks. OAuth remains independent.
- Browser-approved Alembic 0020 MCP catalogue: six reads plus safeguarded `reserve_project`, `consume_reservation`, `cancel_reservation`, `adjust_part_quantity`, `create_part`; global/client policy, `mcp:write`, active Operator+ user, preview/confirmation/idempotency/replay/state-drift defenses apply. Stock/create paths reuse canonical services. OAuth challenges advertise enabled read/write categories; existing tokens require explicit reauthorization. History shows the MCP client name while retaining backing user authority. No-auth stays read-only; live policy/OAuth state is mutable.
- Browser-approved Settings Direct MCP hierarchy: Allow direct clients + No authentication sit with Named direct clients; global Server/Read/Write stay separate. Dependency-disabled uses `not-allowed`; `wait` only while saving.

Recovery lessons
- Rehearsals stay isolated under /tmp; use runtime-safe markers/frozen bytes and canonical Docker modes.
- Durable-log validation must use bytes explicitly written to logs, never terminal-only success/error output. Patches 676 and 678 repeated this mistake; Patch 679 diagnosed it. Prove failures from immutable source plus reproducible read-only facts. Do not freeze mutable live MCP/DB values.

Remaining V1
1. Add guarded inventory metadata edit/correction; delete/restore only with existing recycle-bin/dependency/confirmation safeguards.
2. Dedicated Settings role/user-management UI when prioritized; backend role enforcement already exists.
3. Final public-alpha accessibility/security/responsive/backup-restore/REST-OpenAPI/MCP regression and release checkpoint.
- Notifications & Messaging remain post-v1.

Autosave/live-sync policy
- Reversible Preferences autosave independently; consequential create/edit credentials, password, lifecycle, backup/restore, delete/revoke/rotate actions stay explicit.
- Authenticated SSE invalidation + targeted refetch is the live-update default; reconnect/resync and degraded polling recover transport failures, while Retry remains for real errors.
- MCP UI/runtime tool lists come only from the canonical implemented catalogue.

Chat22 recovery
- Patch 661 recovered a packaged-smoke byte mismatch; Patch 660 recovered Roadmap trailing whitespace. Keep packaged bytes identical to rehearsals and validate docs before writes.

PARTPILOT:INVENTORY_HISTORY_LIVE_SYNC_MEMORY:V699
Chat 24 live-sync milestone — Patch 699
- Browser-approved first slice: Inventory/Part Manager + History.
- Backend: protected `/api/live/events` + `/api/live/state`; process-local broker
  with generation/sequence, replay/resync, topic revisions and lifecycle-aware
  SSE termination. Current one-process topology is supported; multiworker later
  requires shared pub/sub/outbox.
- Frontend: authenticated fetch/ReadableStream SSE; Last-Event-ID reconnect;
  backoff; polling only while degraded; same-origin BroadcastChannel relay with
  event-ID deduplication.
- Part mutations publish `inventory` + `history` only after committed service
  success. Stored Parts keeps local filters/search/sort/page/selection and
  refetches results; open selected-part drawers and movements refresh too.
- History auto-refetch and intermediate-width filter/date layout are approved.
- Manual Refresh/Retry remains during migration. Live sync is not globally
  complete: Dashboard, Projects, Reservations, Settings/account and API/MCP
  administration still require deliberate wiring/browser proof.
- Next after broader live sync: public-alpha OpenAPI/docs hardening, inventory
  metrics + Dashboard Stock alert dialog, roles, safeguarded MCP writes, final
  alpha regression. Notifications remain post-v1.


PARTPILOT:PROJECTS_RESERVATIONS_LIVE_SYNC_MEMORY:V702
Chat 24 live-sync milestone — Patch 702
- Browser-approved second slice: Projects + Reservations.
- Project mutations publish post-commit topics according to whether they are
  draft-only or stock/reservation affecting.
- Reservation mutations publish Reservations/History, add Inventory for
  quantity/lifecycle effects, and add Projects when a linked Project changes.
- Projects refetch list + selected detail on `projects`; Reservations refetch
  list + selected detail + Activity on `reservations`.
- Local filters/search/page/selection remain tab-local; shared authenticated
  SSE + BroadcastChannel delivery remains the transport.
- Inventory/History and Projects/Reservations are approved. Dashboard,
  Settings/account and API/MCP administration still need live-sync wiring.


PARTPILOT:DASHBOARD_LIVE_SYNC_MEMORY:V704
Chat 24 live-sync milestone — Patch 704
- Browser-approved Dashboard inventory live sync.
- Dashboard low-stock summary and an open universal search subscribe to the
  existing `inventory` topic.
- Universal-search text remains local; selected result is retained by ID when
  it still matches after refetch; stale request sequence protection is kept.
- No new backend publisher was needed: approved inventory mutation publication
  remains the source of truth.
- Approved live-sync surfaces now include Inventory/History,
  Projects/Reservations and Dashboard.
- Settings/account and API/MCP administration remain pending live-sync work.


PARTPILOT:SETTINGS_ACCOUNT_LIVE_SYNC_MEMORY:V706
Chat 24 live-sync milestone - Patch 706
- Browser-approved Settings/account/preferences/manual-backup live sync.
- Preferences publish `preferences` + `history`; account mutations publish
  `account` + `history`; manual backup generation publishes `backups` +
  `history`, all after successful committed operations.
- Global appearance/currency/timezone/sidebar identity follow live revisions.
- Dashboard + Part Manager follow the out-of-stock display preference.
- Settings refreshes server-backed preference/account/session/backup state.
- Unsaved Account and Reservation drafts are preserved; active mutation and
  autosave/security/backup workflows defer live reload.
- Approved live-sync surfaces now cover Inventory/History,
  Projects/Reservations, Dashboard and non-credential Settings.
- REST API keys and MCP administration remain the final isolated live-sync
  slice before broader public-alpha hardening.


PARTPILOT:CHAT24_DIAGNOSTIC_BOUNDARY_MEMORY:V710
Chat 24 boundary - Patch 710
- Chat 24 range 686-710 is closed with a diagnostic-only boundary.
- Browser-approved/checkpointed live sync: Inventory/History (699),
  Projects/Reservations (702), Dashboard (704), Settings/account/preferences
  and manual-backup status (706).
- API-key/MCP integration live sync remains pending after consumed 707-709.
- 707 found stale manageable OAuth smoke fields; 708 incorrectly required
  terminal-only failure prose from a durable log; 709 then exposed hard-coded
  historical connected client IDs 9 and 13.
- Clean app state remains Patch 706; no integration browser-test source carries
  across the boundary.
- Chat 25 title: `Chat 25: Integration Live Sync Recovery and Public Alpha
  Hardening`; range 711-735; first patch 711; boundary 735.
- Patch 711 must replace live-ID assumptions with test-owned OAuth connected
  fixtures via register -> consent -> authorization code -> token exchange,
  exact cleanup and copied-DB logical snapshot restoration.


PARTPILOT:INTEGRATION_LIVE_SYNC_MEMORY:V714
Chat 25 live-sync milestone - Patch 714
- Browser-approved API-key/MCP integration live sync completes the current
  authenticated live-sync migration.
- API-key events carry invalidation only; plaintext secrets never cross tabs.
- MCP settings/permissions/OAuth/direct-client state targeted-refetches across
  tabs while unfinished MCP drafts and local credential dialogs are preserved.
- Routine OAuth refresh avoids History noise; consequential lifecycle changes
  invalidate MCP integration state and History as appropriate.
- OAuth smokes are fixture-owned and copied-production-data-safe with exact
  logical restoration.
- MCP still uses explicit `Save changes`; changing that is a separate UX task.
- Next: OpenAPI/docs hardening, inventory metrics, Dashboard Stock alerts,
  roles, safeguarded MCP write tools and final public-alpha regression.



PARTPILOT:MCP_AUTOSAVE_STABLE_REFRESH_MEMORY:V717
Chat 25 UX hardening milestone - Patch 717
- Browser-approved MCP access/tool-permission autosave; manual save/reset controls
  are removed for reversible MCP settings.
- Enabling no-auth still requires `ALLOW NO AUTH`; credential and client
  lifecycle actions remain explicit.
- MCP settings, global permissions, OAuth clients and named direct clients keep
  cached content mounted during background/live refetches; initial loading is
  distinct and cached data is auth-token scoped.
- Cross-tab MCP live sync and local secret/dialog isolation remain intact.
- Audit found the same loading/refetch flash pattern in Projects, Reservations,
  History, Dashboard, Settings preferences/account/backups, API keys and selected
  inventory detail/history. Stabilize those next before broader alpha features.



PARTPILOT:STABLE_BACKGROUND_REFRESH_MEMORY:V719
Chat 25 refresh-stability milestone - Patch 719
- Browser-approved no-flash background refresh now covers Projects,
  Reservations, History, Dashboard, Settings preferences/account/backups, REST
  API keys and selected Stored Parts detail/history.
- Initial loads/data-identity changes may block; same-token/query/selection live
  refetches retain last-known-good content and update it in place.
- Transient background failures keep the visible snapshot and surface errors.
- Main Stored Parts register already used this cache-aware pattern and was not
  changed. Next: OpenAPI/docs, metrics + Stock alert dialog, roles, safeguarded
  MCP writes and final alpha regression.



PARTPILOT:OPENAPI_RESTORE_MEMORY:V723
Chat 25 public-alpha hardening milestone - Patch 723
- Browser-approved Swagger/ReDoc documents Bearer auth, all 43 REST API-key
  operations/scopes, session-only administration and separate MCP OAuth access.
- Restore validation/staging schemas match Alembic 0016.
- Restore logical hashing safely handles SQLite BLOBs; copied-production
  validation/commit/bootstrap/rollback tests pass.
- Next: Stored Parts metrics, Dashboard Stock alert dialog, roles, safeguarded MCP
  write tools and final alpha regression.



PARTPILOT:USER_ROLES_AUTHORIZATION_MEMORY:V733
Chat 25 public-alpha hardening milestone - Patch 733
- Patch 731 Dashboard operational-home/Stock-alert dialog and Patch 728 Stored
  Parts metrics remain checkpointed; routine Refresh controls stay removed.
- Alembic 0017 adds Owner/Administrator/Operator/Viewer without recreating the
  SQLite users table; all pre-existing accounts are backfilled to Owner and
  upgrade/downgrade/re-upgrade preserves every production row/sequence.
- Viewer is operational read-only; Operator adds operational writes;
  Administrator adds workspace/MCP/backup and lower-role user administration;
  Owner additionally controls restore/reset and Owner lifecycle.
- Session-only user APIs support create/list/access changes, disable/reactivate,
  force-reset, session revoke and confirmed delete with last-Owner protection.
- REST API keys are capped by both stored scopes and the owning user's current
  role, preventing stale high-scope keys from surviving demotion as escalation.
- Backup/restore contracts now target 0017. Role-management UI is deferred; next
  are safeguarded MCP write tools and final public-alpha regression.

PARTPILOT:SAFEGUARDED_MCP_WRITES_CHAT25_BOUNDARY_MEMORY:V742
Chat 25 boundary — Patch 742
- Browser-approved lifecycle MCP writes are checkpointed on Alembic 0018: six
  read tools + `reserve_project`, `consume_reservation`, `cancel_reservation`.
- Writes require server + Write authorization + global tool + client ceiling +
  `mcp:write` + active Operator-or-higher user. No-auth is permanently read-only.
- Same-tool preview/confirm uses a five-minute one-time token, idempotency and
  state-drift rejection; committed mutations use MCP audit/movement attribution
  and publish live invalidation. Migration defaults write tools off; live policy
  remains mutable.
- OAuth policy count is not active tool exposure: existing tokens do not gain
  `mcp:write` silently. Reauthorize clients after enabling write scope.
- Client Permissions dialog is viewport-scrollable and labels counts `allowed by
  policy`; OAuth scopes may further reduce active tools.
- Patch 741 boundary validation was consumed because the write smoke froze mutable
  MCP policy to migration defaults; Patch 742 fixes only that copied-test contract.
- Chat 25 nominal range 711-735 overran only for narrow MCP-write recovery. Patch
  742 is the boundary. Next: `Chat 26: MCP Inventory Writes and Public Alpha
  Finalization`, range 743-767, first 743, boundary 767.
- Chat 26: narrow inventory MCP writes (add/edit/stock, then delete/restore only
  with existing safeguards), optional role-management UI, final public-alpha
  regression. Notifications & Messaging remain post-v1.

PARTPILOT:PATCH759_MCP_PART_CREATE_CHECKPOINT_MEMORY
Chat 26 Patch 759 checkpoint
- Browser-approved Alembic 0020 adds `create_part` default-off while preserving prior mutable policy; catalogue = six reads + five guarded writes.
- Creation reuses canonical validation and freezes normalized payload + catalogue/template dependencies into preview before confirmation; idempotency/replay/drift and post-commit inventory/history invalidation remain exact.
- OAuth challenge now requests enabled MCP read/write categories; existing tokens never auto-escalate and Claude write access was browser-proved only after reauthorization.
- History shows OAuth/direct MCP client identity (e.g. Claude) instead of the backing username while retaining `actor_user_id` authority; MCP stock movements stay typed MCP and older matching audits can be hydrated without DB rewrite.
- Copied write smokes isolate fixture-owned intents; legitimate live browser-test OAuth/write-intent/audit/inventory rows remain mutable data.
- Next: guarded metadata edit/correction, then delete/restore only with exact current safeguards; optional user-management UI and final public-alpha hardening before Patch 767.
