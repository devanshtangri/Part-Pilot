<!-- PARTPILOT:PROJECT_MEMORY:V632 -->

Project
- Repo https://github.com/devanshtangri/Part-Pilot.git; root /projects/Part Pilot; branch main.
- Docker compose service partpilot, host port 7890. React/Vite/TypeScript + FastAPI/SQLAlchemy/Alembic.
- Alembic head: 0015_mcp_direct_clients.

Patch workflow
- Every implementation/fix/diagnostic/docs/checkpoint/commit/push/boundary is one complete sequential Python file under fixes/. Failed attempts consume numbers; never reuse. User runs scripts; assistant must not execute numbered patches.
- Success final line exactly `Everything PASS`. Validate exact repo/branch/origin/HEAD, index/source hashes, prerequisites and transforms before writes. Backups fixes/backups/; logs fixes/logs/; diagnostics docs/diagonostic_*.md.
- Browser-test source stays uncommitted until explicit approval. After approval, checkpoint/commit/push promptly. Browser steps are separate from scripts.
- After two consecutive pre-write failures/repeated anchors/source uncertainty, diagnostic-only next. Compile final downloadable patch before delivery.
- App changes: git diff --check, relevant Python compile, canonical Docker build, Alembic, copied-db complete smoke, protected APIs/SPA/runtime markers, live-data preservation and rollback.
- Exact Docker builds require committed .dockerignore plus Git-canonical backend/frontend modes; ignored pyc/filesystem-mode drift previously changed image identity. Never rehearse against live source; use isolated /tmp copies.
- Keep this memory <=8000 bytes.

Chat boundary
- Current: Chat 21 `Account Security and Session Administration`. Browser/recovery work consumed 628,629,631; Patch 632 is the named-direct-client checkpoint. Patch 633 is mandatory boundary/handoff. Chat 22 starts at 634 only after 633 succeeds.
- No starting-prompt files; ready prompt only in chat after successful boundary.

Data safety
- Preserve users/sessions/inventory/Projects/Reservations/movements/audits/settings/API keys/OAuth/direct credentials/backups/restore evidence. Cleanup only exact owned IDs. SQLite integrity/FKs must pass.
- 5V Relay remains recoverable in Deleted items. ESP01 was permanently purged and Development Board Part Type deleted during approved recycle-bin testing.
- Instance-secret SHA: 19c5c9519a188a8f969bb2bd67c65da1418a73605e8747450df8ddeb44d8b47b. Never reveal/rotate credentials automatically.

Completed application
- Authenticated typed inventory; catalogues; universal search/filter/sort/pagination; quantity workflows; soft delete/restore; History.
- Atomic Reservation/Project reserve/release/consume/edit lifecycles.
- Responsive dark/light/system UI and Settings; strict portable backup/restore with staging/rollback/session invalidation.
- Recycle bin: recoverable delete by default, atomic typed-confirm permanent purge, active/reserved blockers, historical audit retention, custom Part Type dependency protection across active + deleted inventory.
- Current-user profile/account security: username/display/avatar incl normalized DB-backed custom avatar, password change, safe sessions, targeted/revoke-all-other, current-session preservation, secret-free audit. Four older sessions remain Unknown and must not be inferred/backfilled.

REST API access
- Alembic 0014 introduced scoped `pp_api_key_` credentials: named high-entropy one-time secrets, digest-only storage, explicit scopes/expiry, rotate/revoke/last-used.
- Patch 617 enforces 43 exact eligible route method/path scope registrations for Inventory/Catalogue/Project/Reservation/History. Browser sessions fall back normally; Auth/Settings/Backup/Restore remain session-only. Invalid/revoked/expired=401; missing scope=403.
- API Access Settings UI is browser-approved/checkpointed: create/edit/rotate/revoke, hidden revoked records, one-time secret, scope presets, API docs actions and modal security flows. `/docs`, `/redoc`, `/openapi.json` still need deliberate public-alpha access policy.

MCP/OAuth
- OAuth discovery, DCR, PKCE consent, access/refresh/revocation, protected-resource metadata and stateless `/mcp`; six read-only Part/Project/Reservation tools. Real Claude/ChatGPT read flows verified.
- Manual OAuth registration/admin supports creator ownership, public/confidential, none/secret_post/secret_basic, generated ID, one-time secret digest-only storage, manageable status and exact revocation. Revoked clients hidden from normal Settings.
- Alembic 0015 replaces singleton direct-auth administration with named Bearer/custom-header/trusted-network clients. Each client has independent identity, enable/disable, rotate/revoke and safe usage metadata. Legacy disabled row is retained for audit compatibility.
- `Enable MCP server` is master. `Allow direct MCP clients` gates named direct access. Instance-wide no-auth is off by default, typed-confirmed with `ALLOW NO AUTH`, read-only, and lower precedence than explicit credentials/trusted networks. OAuth remains independent.
- Browser-approved MCP UI has named-client security dialogs, aligned section widths and one readable muted disabled state. Direct clients/no-auth should remain OFF unless deliberately configured. MCP write authorization remains OFF.

Recent recovery lessons
- 628: CSS comment runtime marker stripped by minification; use DOM/value/custom-property markers.
- 629: frozen packaged CSS hash differed from rehearsal; freeze exact script-produced bytes.
- 631: preflight correctly detected live source already at intended final V631 bytes because a rehearsal accidentally touched live source. Never mutate live repo/deployment during rehearsal; isolated /tmp only.

Remaining V1
1. Global individual-tool + per-client MCP permissions.
2. Broader Settings organization with restrained dividers/groups throughout relevant Settings areas.
3. Protect API docs/schema exposure before public alpha.
4. ISO currency selector using persisted app-wide currency; formatting only, no FX.
5. Server-backed Stored Parts metrics across complete active inventory: Total components=sum total_quantity; Inventory value=sum priced total_quantity*unit_price with explicit price coverage; Available, Reserved, Low stock, Out of stock, distinct Part count.
6. Preference/default restoration; multi-user Owner/Admin/Operator/Viewer roles; safeguarded MCP writes; final accessibility/security/responsive/API-MCP regression.
- Notifications & Messaging remain post-v1.
