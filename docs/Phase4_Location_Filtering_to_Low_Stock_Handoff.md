# Part Pilot — Location Filtering to Low-Stock Handoff

Generated by Patch 173 after terminal verification and the user’s browser
approval of Patch 171.

## Repository

- GitHub: `https://github.com/devanshtangri/Part-Pilot`
- Local project path: `/projects/Part Pilot`
- Branch: `main`
- Alembic head/current: `0005_packages`
- Latest implementation commit:
  `bdfedec528449d0443a1fec32e0be90e31b9923e` (`bdfedec`)
  — `Show and filter inventory by location`
- Documentation checkpoint commit subject:
  `Update location filtering checkpoint and public README`

The checkpoint script pushes both commits before reporting success.

## Browser approval carried into this handoff

The user’s exact response after running Patch 171 and testing the Stored Parts
location workflow was:

```text
everything pass
```

The frontend implementation was intentionally left uncommitted until that
approval.

## Completed location workflow

### Reusable catalogue

- Protected location list/create/update/delete API.
- Normalised duplicate-name protection.
- Optional notes and active/deleted usage counts.
- HTTP 409 protection while active or deleted parts reference a location.
- Audit events for location creation, update, and deletion.

### Part integration

- Optional `location_id` in part creation and metadata editing.
- `location_id` and `location_name` in active and deleted part responses.
- Shared selector in Add Part and Edit details.
- Inline location creation and immediate selection.
- Existing-value preselection, reassignment, and clearing.
- Location retained through soft deletion and restoration.
- Audit snapshots include location context.

### Stored Parts filtering

- Optional protected `location_id` on `GET /api/parts`.
- Correct filtered totals and pagination.
- Combined part-type and location filtering.
- Deleted rows remain excluded.
- Unassigned parts remain in the unfiltered collection.
- Frontend `getParts()` accepts `locationId`.
- Reusable **All locations** selector.
- Backend-driven inventory reload when location changes.
- Location names participate in text search.
- Counts identify the selected location.
- Correct filter-empty and clear-filter behaviour.
- Location column and Part Details display.
- Responsive desktop, tablet, and mobile toolbar styling.

## Verification completed

- Python compilation where applicable.
- TypeScript/Vite production build through Docker.
- `git diff --check`.
- Docker Compose deployment.
- Health route.
- Alembic source head and deployed current revision.
- Complete `app.db.smoke_test` suite.
- Protected API route checks.
- Deployed frontend bundle-marker verification.
- `/part-manager` SPA route verification.
- Manual browser verification.

## Current project state

Roadmap-wide V1 estimate: **approximately 53% complete**.

Implemented major slices include:

- Repository, database, migrations, setup, authentication, and application
  shell.
- Built-in and custom part types.
- Dynamic Add Part workflow.
- Manufacturer, package, and location catalogues.
- Stored Parts browsing, search, stock filters, details, and location filter.
- Quantity adjustments and recent movement history.
- Existing-part metadata editing.
- Recoverable deletion and restoration.

Major remaining V1 areas include low-stock/dashboard behaviour, universal
search completion, reservations, projects, full history browsing, settings,
backups, MCP, and release polish.

## Next chat: start with Diagnostic 174

The next implementation area is **low-stock and settings-driven out-of-stock
behaviour**.

Diagnostic 174 must be read-only and should inspect:

1. Low-stock threshold fields and existing calculation helpers.
2. Dashboard component boundaries and placeholders.
3. Existing `app_settings` rows, service helpers, and missing protected routes.
4. The locked product decision:
   in-stock results first, followed by a distinct Out of Stock section, with
   visibility controlled by settings.
5. Stored Parts, dashboard, and future universal-search integration points.
6. Existing loading, error, empty, responsive, and accessibility patterns.
7. Smoke-test cleanup conventions.
8. The smallest safe backend and frontend implementation batches.

Do not combine this slice with reservations, projects, backups, MCP, or a full
search rewrite.

## Mandatory working rules

- Every fix or implementation is delivered as one complete Python file.
- Do not require manual line editing or copy/paste changes.
- Preflight all source transformations in memory before backups and writes.
- Back up every changed file.
- Verify prerequisites and exact intended file sets.
- Compile Python where applicable.
- Run `git diff --check`.
- Build and deploy Part Pilot when application source changes.
- Run the complete smoke suite.
- Verify deployed frontend bundles and relevant routes.
- Roll back safely on failure.
- Print the exact line `Everything PASS` last and only after complete success.
- Browser-dependent work remains uncommitted until the user approves it.
- Keep implementation and documentation commits separate.
- Update `docs/Checkpoint.md` and `docs/Implementation_Roadmap.md` at meaningful
  checkpoints.
- Preserve unrelated untracked files.
- Check `git status` before assuming GitHub and the local tree are identical.
- Prefer small, independently verifiable patches.

## Files to read first in the next chat

Read these from the `main` branch on GitHub before proposing Patch 174:

1. `docs/Phase4_Location_Filtering_to_Low_Stock_Handoff.md`
2. `docs/Checkpoint.md`
3. `docs/Implementation_Roadmap.md`
4. `README.md`
5. Relevant current backend/frontend source discovered by Diagnostic 174

Then ask for or inspect:

```bash
cd "/projects/Part Pilot"
git status --short --branch
git log -4 --oneline
```

Treat the repository and this handoff as the durable source of truth.
