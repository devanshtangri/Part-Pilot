# Part Pilot — Inventory Details and Package Catalogue Handoff

Generated by Patch 131 after automated verification and manual browser approval.

## Source of truth

- Repository: `https://github.com/devanshtangri/Part-Pilot`
- Branch: `main`
- Checkpoint commit subject: `Add part details and package catalogue`
- Current Alembic head: `0005_packages`
- Direct handoff source: `https://raw.githubusercontent.com/devanshtangri/Part-Pilot/main/docs/Phase4_Inventory_Details_Package_Catalogue_Handoff.md`

A new chat must fetch this file from GitHub and inspect the current repository
before changing anything.

## Mandatory workflow

Every diagnostic, fix, or implementation must be supplied as a complete Python
file. Never ask the user to find lines or edit source manually.

Every implementation script must:

1. Verify exact prerequisites and tracked status.
2. Back up every file it changes.
3. Keep the patch narrowly scoped.
4. Compile Python and build the frontend.
5. Run `git diff --check`.
6. Build and deploy Part Pilot.
7. Verify Alembic.
8. Run the complete smoke suite.
9. Verify deployed bundle markers and routes.
10. Roll back on failure.
11. Print the exact final line `Everything PASS` only after every intended
    operation and verification succeeds.

Do not commit implementation work until the user confirms browser testing.
At meaningful checkpoint commits, update `docs/Checkpoint.md`,
`docs/Implementation_Roadmap.md`, and the active handoff.

## Repository context

- Local path: `/projects/Part Pilot`
- Docker service: `partpilot`
- Browser route: `/part-manager`
- Default exposed port: `7890`
- Database: SQLite in the persistent data volume
- Migration head: `0005_packages`

Migration chain:

- `0001_database_foundation`
- `0002_schema_hardening`
- `0003_user_display_name`
- `0004_manufacturers`
- `0005_packages`

## Completed foundations

### Database and authentication

- Core V1 SQLAlchemy models exist.
- SQLite foreign keys are enforced.
- Built-in part types: 34.
- Template fields: at least 157.
- Default app settings: 17.
- Single-user setup and authentication are complete.
- Application and API routes are protected.

### Part types and custom fields

- Built-in templates are seeded.
- Custom part types can be created, edited, and safely deleted when unused.
- Manufacturer is available as a semantic field preset.
- Duplicate Manufacturer and Package controls are suppressed in Add Part.
- Template-field rows have consistent visual height.

### Inventory creation

- Authenticated part creation is complete.
- Dynamic fields support text, number, Boolean, dropdown, URL, and unit-aware
  validation.
- Manufacturer selection and inline creation are complete.
- Package/form-factor selection and inline creation are complete.
- Successful creation uses a compact confirmation card.

### Inventory browsing and search

- Stored Parts table is implemented.
- Search covers name, part number, type, and manufacturer.
- All, In stock, Low, and Out filters are implemented.
- Inventory refreshes after successful creation.
- Contextual Add Part exists beside the selected template.

### Read-only part details

- Stored Parts rows open a details view.
- Desktop uses a right-side drawer.
- Narrow screens use a bottom sheet.
- Rows support mouse, Enter, and Space.
- Details close through X, Close, Escape, or backdrop.
- Details show identity, type, manufacturer, package, available/reserved/total
  quantities, low-stock settings, unit price, purchase link, description,
  notes, timestamps, and populated template fields.
- Numeric values remove unnecessary trailing zeros without rounding.

### Package/form-factor catalogue

- Alembic `0005_packages` adds a reusable catalogue.
- Common electronics package and module formats are seeded.
- Existing non-empty `Part.package` values are backfilled.
- `Part.package` remains the compatibility text storage contract.
- Protected APIs:
  - `GET /api/packages`
  - `POST /api/packages`
- Normalised duplicate names return a conflict.
- Custom creation writes an audit event.
- Add Part immediately selects newly created package options.

## Browser verification completed

The user manually confirmed:

- part details work
- responsive close behaviour works
- numeric formatting is clean
- seeded package options appear
- custom package creation and reuse work
- package selection appears in details
- only one package control appears
- template-field rows are visually consistent

## Git state after Patch 131

Patch 131 commits and pushes this approved batch. The tracked working tree must
be clean after completion. Local untracked files under `fixes/` may remain and
are not application changes.

## Next implementation area

Start with **Diagnostic 132** for stock movement and quantity adjustment.

Inspect:

- `StockMovement` in `backend/app/models/core.py`
- movement constants in `backend/app/db/constants.py`
- current part services and routes
- audit-log patterns
- quantity fields and validation
- current details-drawer actions and layout
- smoke-test insertion and cleanup patterns
- checkpoint and roadmap requirements

Recommended first implementation boundary after the diagnostic:

- authenticated quantity-adjustment endpoint
- operations: add, remove, consume, correction
- prevent a negative resulting quantity
- record quantity before, quantity after, delta, reason, note, actor, source,
  and timestamp
- expose recent movement history read-only
- add one compact action from the details view
- do not mix reservations, projects, metadata editing, or deletion into the
  same patch

## Explicitly deferred

- editing existing part metadata
- soft deletion and restoration
- reusable locations
- reservations and projects
- dashboard low-stock workflow
- settings completion
- backups
- MCP
- release packaging

## First deliverable in the next chat

Create a complete read-only Python file named:

`132_inspect_stock_movement_targets.py`

It must inspect the live repository and print `Everything PASS` only after the
diagnostic report is successfully written.
