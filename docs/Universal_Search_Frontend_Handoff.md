<!-- PARTPILOT:DASHBOARD_UNIVERSAL_SEARCH_HANDOFF:V224 -->
# Part Pilot — Dashboard Universal Search Handoff

Generated by
`224_checkpoint_commit_push_dashboard_universal_search.py`
at `2026-07-26T17:50:55.233064+00:00`.

## Repository

- GitHub: `https://github.com/devanshtangri/Part-Pilot.git`
- Local root: `/projects/Part Pilot`
- Branch: `main`
- Base before Patch 224: `5b98a8e5f6a74f4fd1923c098a6d1300b9ff86cb`
- Patch 224 commit subject: `Add Dashboard universal inventory search`
- Alembic head: `0005_packages`
- Docker service: `partpilot`
- Typical local port: `7890`

After Patch 224 prints `Everything PASS`, local `main`, `origin/main`, and the
remote branch all point to the Dashboard universal-search checkpoint.

## Read first

1. `docs/Universal_Search_Frontend_Handoff.md`
2. `docs/Universal_Search_Backend_Handoff.md`
3. `docs/Checkpoint.md`
4. `docs/Implementation_Roadmap.md`
5. `README.md`
6. Any newer relevant `docs/*Handoff*.md`

## Completed frontend search

The Dashboard now includes:

- a real search launcher;
- an accessible modal search workspace;
- typed backend `search` support;
- 280 ms live search;
- stale-response invalidation;
- loading, error, empty, available, hidden, and out-of-stock states;
- settings-driven out-of-stock visibility;
- selected-result details;
- `/` launch and `Escape` close;
- a fixed SVG close icon;
- populated-section-only rendering;
- visually separated Available and Out of stock cards;
- responsive desktop and mobile layouts.

## Browser-approved design decisions

- Do not require a Search button.
- Search automatically after a brief pause.
- Do not show empty result sections.
- Keep section names concise: `Available` and `Out of stock`.
- Distinguish stock groups visually without requiring close reading.
- Keep out-of-stock matches below available matches.
- Keep the hidden-results notice when Search settings suppress out-of-stock
  rows.
- Keep the selected-result details panel.
- Use a geometry-centred SVG for the close icon.
- Keep mobile summary and low-stock cards compact.

## Durable-memory rule

`docs/Checkpoint.md`, `docs/Implementation_Roadmap.md`, and handoff files are
secondary project memory. Update them at meaningful checkpoints and commits,
not for every small cosmetic patch.

## Immediate next task

The next operation number is **225**.

Migrate Stored Parts search from client-side filtering over the currently
loaded page to the backend universal-search contract.

Recommended boundary:

- send `search` through `getParts`;
- preserve part-type and location filters;
- preserve pagination totals;
- preserve available-before-out-of-stock ordering;
- preserve the out-of-stock visibility preference;
- preserve mobile Inventory layout;
- keep part selection, editing, creation, and management behaviour unchanged;
- use a read-only preflight before issuing source transformations.

## Workflow rules

- Every implementation, fix, diagnostic, checkpoint, commit, push, and handoff
  is supplied as a complete downloadable Python script.
- Continue the single sequential patch numbering scheme.
- A successful script prints exactly `Everything PASS` as its final line.
- Browser-test implementations remain uncommitted until explicit browser
  approval.
- Terminal `Everything PASS` is automated verification, not browser approval.
- Do not include browser-test instructions inside patch scripts.
- Commit and push each independently approved batch promptly.
